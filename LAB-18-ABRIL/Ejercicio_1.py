from random import randint


def Matriz_1 (A,B):
        lista = [0]*A
        for x in range(len(lista)):
                lista[x]= [0]*B
        for z in range(len(lista)):
         print (lista[z])
A=Matriz_1(5,5)

print("-------------")
def Matriz_2 (A,B):
        lista = [0]*A
        for x in range(len(lista)):
                lista[x]= [0]*B 
        for z in range(len(lista)):
                print (lista[z])
        
A=Matriz_2(5,5)